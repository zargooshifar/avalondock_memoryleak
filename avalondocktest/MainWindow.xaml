<Window x:Class="avalondocktest.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:avalondocktest"
        xmlns:avalonDock="https://github.com/Dirkster99/AvalonDock"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel>
            <Button Click="Button_Click">add new dock</Button>
            <Button Click="Button2_Click">create and close 10000 docks</Button>

        </StackPanel>
        <avalonDock:DockingManager Grid.Row="1" Name="dockManager"
                                    AnchorablesSource="{Binding DockItems}">
            <avalonDock:DockingManager.LayoutItemContainerStyle>
                <Style TargetType="{x:Type avalonDock:LayoutAnchorableItem}">
                    <Setter Property="Title" Value="{Binding Title}" />
                    <Setter Property="ContentId" Value="{Binding Title}" />
                    <Setter Property="CanClose" Value="True" />
                    <Setter Property="CanHide" Value="False" />
                </Style>
            </avalonDock:DockingManager.LayoutItemContainerStyle>
            <avalonDock:DockingManager.LayoutItemTemplate>
                <DataTemplate>
                    <local:UserControl1 />
                </DataTemplate>
            </avalonDock:DockingManager.LayoutItemTemplate>
        </avalonDock:DockingManager>
    </Grid>

</Window>
